[package]
name = "bkndb-storage-redb"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Redb-backed storage engine backend for BknDb"
readme = "README.md"

[dependencies]
bkndb-core = { version = "0.1.0", path = "../bkndb-core" }
redb = { workspace = true }

[dev-dependencies]
bkndb-core = { path = "../bkndb-core", features = ["test-util", "graph", "relational"] }
tempfile = "3"
